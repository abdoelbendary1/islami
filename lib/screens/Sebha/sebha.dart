import 'package:flutter/material.dart';

class SebhaScreeen extends StatelessWidget {
  const SebhaScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}
